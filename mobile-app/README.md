# BudgeMe Mobile App

Expo mobile app for BudgeMe.

## Local Commands

```powershell
npm.cmd run typecheck
npm.cmd run start
```

## Current Baseline

- Expo Router shell under `app/`
- App providers under `src/app/providers`
- Tamagui theme under `src/theme`
- Feature slices start under `src/features`
